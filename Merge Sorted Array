class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        ArrayList<Integer> q=new ArrayList<>();
for(int i=0;i<m;i++){
    q.add(nums1[i]);
}
for(int i=0;i<n;i++){
    q.add(nums2[i]);
}
Collections.sort(q);

int y=0;
for(int i:q)
nums1[y++]=i;
   
    }
}
